export type Role = 'student' | 'teacher' | 'school_admin' | 'platform_admin';
export type Session = { profileId:string; role:Role; organizationId:string|null; displayName:string; locale:'vi'|'en' };
const CAPABILITIES: Record<Role,string[]>={student:['class:read:own','activity:read:assigned','activity:submit:own','progress:read:own','resource:read:assigned','note:write:own'],teacher:['class:read:assigned','roster:read:assigned','assignment:write:assigned','submission:read:assigned','feedback:write:assigned','attendance:write:assigned','progress:read:assigned','report:read:assigned','announcement:write:assigned'],school_admin:['org:read:own','org:write:own','class:write:org','teacher:manage:org','student:manage:org','credential:issue:org','report:read:org','integration:manage:org','billing:read:org'],platform_admin:['*']};
export function can(session:Session|null,capability:string){if(!session)return false;const caps=CAPABILITIES[session.role]??[];return caps.includes('*')||caps.includes(capability)}
export function requireCapability(session:Session|null,capability:string){if(!can(session,capability)){const err:any=new Error(`Forbidden: missing capability "${capability}"`);err.status=403;throw err}return session as Session}
export function assertSameOrg(session:Session,organizationId:string){if(session.role==='platform_admin')return;if(session.organizationId!==organizationId){const err:any=new Error('Forbidden: cross-organization access');err.status=403;throw err}}
